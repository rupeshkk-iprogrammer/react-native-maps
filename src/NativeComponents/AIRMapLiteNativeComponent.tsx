import { requireNativeComponent } from 'react-native';
import { MapViewProps } from 'react-native-maps';

const AIRMapLite = requireNativeComponent<MapViewProps>('AIRMapLite');

export default AIRMapLite;
